#!/usr/bin/python3
"""Script that defines the State class."""
from models.base_model import BaseModel


class State(BaseModel):
    """attribute:
        name : (empty str)represent the name of the state.
    """

    name = ""
