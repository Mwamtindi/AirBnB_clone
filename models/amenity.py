#!/usr/bin/python3
"""Script that defines the Amenity class."""
from models.base_model import BaseModel


class Amenity(BaseModel):
    """attribute:
        name : (empty str) reprsent the name of the amenity.
    """

    name = ""
